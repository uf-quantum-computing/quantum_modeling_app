// ================ MainLayout =================
// sets up the main layout for the app

// ====== imports ======
import { Outlet } from "react-router-dom";

const MainLayout = () => {
  return (
    <Outlet />
  );
};

export default MainLayout;
